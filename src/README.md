#This is the directory where your source code would reside.
Please run it with the below command providing
1. input path
2. input log file name
3. output path


# From edgar-analytics-master
./run.sh `pwd` log.csv `pwd`


# From insight_testsuite
./run_tests.sh `pwd`/tests/test_1 log.csv `pwd`/temp